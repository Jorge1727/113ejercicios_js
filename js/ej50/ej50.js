//Conseguir
//Create a function timesFive that accepts one argument, multiplies it by 5, and returns the new value.

//Resolución
function timesFive(num){
    return num * 5;
}

const resultado = timesFive(2);

valueEj50.append(resultado);